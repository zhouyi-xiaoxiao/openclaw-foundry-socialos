# Auth Strategy

- OpenClaw runtime profile: `socialos`
- Secrets supplied via environment or local profile store
- No tokens committed into repo
- Platform APIs are optional in P0/P1; L1 assisted flow works without keys
