# SocialOS runtime defaults
PUBLISH_MODE=dry-run
EMBEDDINGS_PROVIDER=auto
OPENAI_API_KEY=
OPENAI_EMBEDDING_MODEL=text-embedding-3-small
LOCAL_EMBEDDING_MODEL=strong
DUAL_INDEX=0

# Optional live publish gate (requires dashboard toggle + credentials too)
# PUBLISH_MODE=live
