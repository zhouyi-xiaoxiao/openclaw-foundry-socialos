# SocialOS Runtime

Product workspace for local-first SocialOS:
- `apps/web` dashboard (P0-4)
- `apps/api` local API (P0-3)
- `openclaw/` runtime profile, skills, plugin schemas
- `docs/` product/architecture/safety/auth/embeddings docs

Current status: P0-1 delivered (runtime skeleton + policy rails).