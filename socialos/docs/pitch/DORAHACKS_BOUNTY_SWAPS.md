# SocialOS DoraHacks Bounty Modules

Use one of these `60-120 second` modules inside each bounty video.

## Claw for Human
Local route:

- `http://127.0.0.1:4173/demo`

Public close:

- `https://zhouyixiaoxiao.org/hackathon/#bounty-claw-for-human`
- `https://zhouyixiaoxiao.org/data/proofs/claw-for-human.json`

Say:

"For Claw for Human, the key point is translation. OpenClaw is real infrastructure underneath this product, but the user does not have to live in a shell or raw trace view to benefit from it. SocialOS turns that capability into one guided relationship workspace with memory, drafts, queue handoff, and reflection in a single loop. The technical proof is the OpenClaw-backed runtime plus the route-aware proof cards. The product proof is that the user sees a calm interface with a real trust boundary."

## Animoca Bounty
Local route:

- `http://127.0.0.1:4173/hackathon?bounty=animoca`

Public close:

- `https://zhouyixiaoxiao.org/hackathon/#bounty-animoca`
- `https://zhouyixiaoxiao.org/data/proofs/animoca.json`

Say:

"For Animoca, the important proof is persistence. SocialOS keeps linked people, identities, interactions, events, and agent lanes across time. So this is not a one-shot assistant task. It is a persistent identity and memory system for creator, community, and operator workflows. The technical implementation is the linked data model plus proof-aware route rendering, and the live proof on this page shows how the same person or context remains reusable across the product loop."

## Human for Claw
Local route:

- `http://127.0.0.1:4173/buddy`

Public close:

- `https://zhouyixiaoxiao.org/hackathon/#bounty-human-for-claw`
- `https://zhouyixiaoxiao.org/data/proofs/human-for-claw.json`

Say:

"For Human for Claw, the product lens changes from power to trust. Buddy mode narrows the system into four safe tasks: meeting new people, remembering context, generating gratitude or follow-up notes, and calm reflection. No open publish lane, no confusing configuration surface, and no hidden escalation. The technical implementation reuses the same backend but exposes a safer route and tighter boundaries for first-time or younger users."

## Z.AI General
Local route:

- `http://127.0.0.1:4173/hackathon?bounty=z-ai-general`

Public close:

- `https://zhouyixiaoxiao.org/hackathon/#bounty-z-ai-general`
- `https://zhouyixiaoxiao.org/data/proofs/z-ai-general.json`

Say:

"For Z.AI General, GLM is part of the real SocialOS workflow. I can trigger a live GLM generation run, show the provider and model metadata, and then connect that same provider path to product summaries and draft generation inside the product. This is important because the integration is not isolated from the product story. GLM is being used exactly where SocialOS needs production-ready generation and reasoning."

## AI Agents for Good
Local route:

- `http://127.0.0.1:4173/hackathon?bounty=ai-agents-for-good`

Public close:

- `https://zhouyixiaoxiao.org/hackathon/#bounty-ai-agents-for-good`
- `https://zhouyixiaoxiao.org/data/proofs/ai-agents-for-good.json`

Say:

"For AI Agents for Good, SocialOS becomes a coordination system for impact work. The live FLock route adds SDG labeling, urgency, and a next action recommendation, and then SocialOS keeps that result inside a longer-running relationship and follow-up loop. On top of that, the same workflow can hand the next action into outreach lanes and the Telegram volunteer channel. The point is not just answering an impact question once, but carrying the result into real volunteer, student, or community coordination."
